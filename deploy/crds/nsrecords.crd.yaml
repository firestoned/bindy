# Copyright (c) 2025 Erick Bourgeois, firestoned
# SPDX-License-Identifier: MIT
#
# This file is AUTO-GENERATED from src/crd.rs
# DO NOT EDIT MANUALLY - Run `cargo run --bin crdgen` to regenerate
#
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: nsrecords.bindy.firestoned.io
spec:
  group: bindy.firestoned.io
  names:
    categories: []
    kind: NSRecord
    plural: nsrecords
    shortNames: []
    singular: nsrecord
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NSRecord delegates a subdomain to authoritative nameservers. Used for subdomain delegation to different DNS providers or servers.
        properties:
          spec:
            description: |-
              `NSRecord` delegates a subdomain to other nameservers.

              NS (Nameserver) records specify which DNS servers are authoritative for a subdomain. They are used for delegating subdomains to different nameservers.

              # Example

              ```yaml apiVersion: dns.firestoned.io/v1alpha1 kind: NSRecord metadata: name: subdomain-ns spec: zone: example.com name: subdomain nameserver: ns1.other-provider.com. ttl: 86400 ```
            properties:
              name:
                description: Subdomain to delegate. For zone apex, use "@".
                type: string
              nameserver:
                description: |-
                  Fully qualified domain name of the nameserver.

                  Must end with a dot. Example: "ns1.example.com."
                type: string
              ttl:
                description: Time To Live in seconds.
                format: int32
                maximum: 2147483647.0
                minimum: 0.0
                nullable: true
                type: integer
              zone:
                description: DNS zone this record belongs to.
                type: string
            required:
            - name
            - nameserver
            - zone
            type: object
          status:
            description: Generic record status
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Condition represents an observation of a resource's current state.

                    Conditions are used in status subresources to communicate the state of a resource to users and controllers.
                  properties:
                    last_transition_time:
                      description: Last time the condition transitioned from one status to another (RFC3339 format).
                      nullable: true
                      type: string
                    message:
                      description: Human-readable message indicating details about the transition.
                      nullable: true
                      type: string
                    reason:
                      description: Brief CamelCase reason for the condition's last transition.
                      nullable: true
                      type: string
                    status:
                      description: 'Status of the condition: True, False, or Unknown.'
                      type: string
                    type:
                      description: 'Type of condition. Common types include: Ready, Available, Progressing, Degraded, Failed.'
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              observed_generation:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: NSRecord
        type: object
    served: true
    storage: true
    subresources:
      status: {}
