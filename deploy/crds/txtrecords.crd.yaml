# Copyright (c) 2025 Erick Bourgeois, firestoned
# SPDX-License-Identifier: MIT
#
# This file is AUTO-GENERATED from src/crd.rs
# DO NOT EDIT MANUALLY - Run `cargo run --bin crdgen` to regenerate
#
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: txtrecords.bindy.firestoned.io
spec:
  group: bindy.firestoned.io
  names:
    categories: []
    kind: TXTRecord
    plural: txtrecords
    shortNames: []
    singular: txtrecord
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TXTRecord stores arbitrary text data in DNS. Commonly used for SPF, DKIM, DMARC policies, and domain verification.
        properties:
          spec:
            description: |-
              `TXTRecord` holds arbitrary text data.

              TXT records are commonly used for SPF, DKIM, DMARC, domain verification, and other text-based metadata.

              # Example

              ```yaml apiVersion: dns.firestoned.io/v1alpha1 kind: TXTRecord metadata: name: spf-example-com spec: zone: example.com name: "@" text: - "v=spf1 include:_spf.google.com ~all" ttl: 3600 ```
            properties:
              name:
                description: Record name within the zone.
                type: string
              text:
                description: |-
                  Array of text strings. Each string can be up to 255 characters.

                  Multiple strings are concatenated by DNS resolvers. For long text, split into multiple strings.
                items:
                  type: string
                type: array
              ttl:
                description: Time To Live in seconds.
                format: int32
                maximum: 2147483647.0
                minimum: 0.0
                nullable: true
                type: integer
              zone:
                description: DNS zone this record belongs to.
                type: string
            required:
            - name
            - text
            - zone
            type: object
          status:
            description: Generic record status
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Condition represents an observation of a resource's current state.

                    Conditions are used in status subresources to communicate the state of a resource to users and controllers.
                  properties:
                    last_transition_time:
                      description: Last time the condition transitioned from one status to another (RFC3339 format).
                      nullable: true
                      type: string
                    message:
                      description: Human-readable message indicating details about the transition.
                      nullable: true
                      type: string
                    reason:
                      description: Brief CamelCase reason for the condition's last transition.
                      nullable: true
                      type: string
                    status:
                      description: 'Status of the condition: True, False, or Unknown.'
                      type: string
                    type:
                      description: 'Type of condition. Common types include: Ready, Available, Progressing, Degraded, Failed.'
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              observed_generation:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: TXTRecord
        type: object
    served: true
    storage: true
    subresources:
      status: {}
