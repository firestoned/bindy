# Documentation build artifacts
target/
book/

# mdBook build output (not the source!)
# Note: book source is in docs/src/ and should be tracked
# Only ignore the build output in docs/target/ and docs/book/
# (mdbook serve creates book/ despite build-dir setting)

# Temporary files
*.tmp
*.swp
*~
